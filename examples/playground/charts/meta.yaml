# Default source and board layout for all playground example dashboards.
# Uses the pre-built DuckDB database with sales, products, users, etc.
source: examples_db

style:
  frame:
    max_width: 1200.0
