kind: Fixes
body: A variable's `min`, `max` or `step` must be a finite number. `.inf` or `.nan` published a bound the control read back as `NaN`, so the board now errors at parse time instead of rendering a control that ignores it.
time: 2026-09-14T14:22:15.381292-07:00
